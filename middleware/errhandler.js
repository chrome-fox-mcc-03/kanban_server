module.exports = function(err, req, res, next) {
    if(err.name) {

    }
    else {
        switch (err.message) {
            case "":
                
                break;
        
            default:
                break;
        }
    }
}